export const env: {
  port: number;
  mongoUri: string;
  jwtSecret: string;
  jwtExpiresIn: string;
  allowedOrigins: string[];
  geminiApiKey: string;
  geminiModel: string;
  adminUserId: string;
  adminPassword: string;
};
